services:
  - type: web
    name: acpl-assistant
    env: python
    buildCommand: "pip install -r requirements.txt && python prepare.py"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"